<p id="notice"><%= notice %></p>

<h1>Listing Guest Infos</h1>

<table>
  <thead>
    <tr>
      <th>First name</th>
      <th>Last name</th>
      <th>Address</th>
      <th>Email</th>
      <th>Phone number</th>
      <th>Latitude</th>
      <th>Longitude</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @guest_infos.each do |guest_info| %>
      <tr>
        <td><%= guest_info.first_name %></td>
        <td><%= guest_info.last_name %></td>
        <td><%= guest_info.address %></td>
        <td><%= guest_info.email %></td>
        <td><%= guest_info.phone_number %></td>
        <td><%= guest_info.latitude %></td>
        <td><%= guest_info.longitude %></td>
        <td><%= link_to 'Show', guest_info %></td>
        <td><%= link_to 'Edit', edit_guest_info_path(guest_info) %></td>
        <td><%= link_to 'Destroy', guest_info, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Guest info', new_guest_info_path %>
